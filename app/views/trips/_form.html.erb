<%= form_for(@trip) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div id="form">

    <div class="field">
      <%= f.text_field :on_station %>
      <div class="l">Leaving from</div>

    </div>
    <div class="field">
      <%= f.text_field :on_station_time%></div>
      <div class="l">Time</div>


    <div class="field">
      <%= f.text_field :off_station %></div>
    <div class="l">Arriving at</div>

    <div class="field">
      <%= f.number_field :wifi_duration %>
    </div>
    <div class="l">Duration need</div>

    <div class="actions">
      <%= f.hidden_field :user_id, :value => current_user.id %>
      <%= f.submit %>
    </div>
  <% end %>
