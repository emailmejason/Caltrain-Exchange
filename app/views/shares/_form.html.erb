<%= form_for(@share) do |f| %>
  <% if @share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@share.errors.count, "error") %> prohibited this share from being saved:</h2>

      <ul>
      <% @share.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class id="form">
  <div class="field">
    <%= f.text_field :network_name %>
    <div class="l">Network Name</div>

  </div>
  <div class="field">
    <%= f.text_field :network_password %>
    <div class="l">Network ID</div>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>