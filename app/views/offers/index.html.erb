</br>
</br>
</br>
<h1>Listing offers</h1>

<table>
  <tr>
    <th>User id</th>
    <th>User Email</th>
    <th>Train Number</th>
    <th>On station</th>
    <th>Off station</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @offers.each do |offer| %>
  <tr>
    <td><%= offer.user_id %></td>

    <td><%= User.find(offer.user_id).email %></td>
    <td><%= Train.find(offer.train_id).number %></td>
    <td><%= offer.on_station %></td>
    <td><%= offer.off_station %></td>
    <td><%= link_to 'Show', offer %></td>
    <td><%= link_to 'Edit', edit_offer_path(offer) %></td>
    <td><%= link_to 'Destroy', offer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Offer', new_offer_path %>
